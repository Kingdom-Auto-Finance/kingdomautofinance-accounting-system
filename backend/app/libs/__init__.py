"""Library package for backend utilities (pure functions, no I/O).

Contains modules that are safe to import from services, API routes, tests,
and CLI scripts alike. Code here should have no side effects at import time.
"""
